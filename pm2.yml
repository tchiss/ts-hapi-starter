apps:
  - script   : ./dist/Server.js
    name     : 'ts-hapi-starter'
    instances: 1
    exec_mode: cluster
    watch: false
    env:
      NODE_ENV: development
    env_production:
      NODE_ENV: production
